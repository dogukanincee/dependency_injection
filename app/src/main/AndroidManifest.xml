<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReminderApp">

        <!-- Add the following activities to the manifest -->

        <activity
            android:name=".ReminderActivity"
            android:exported="true"
            android:theme="@style/Theme.ReminderApp.Toolbar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".AddReminderActivity"
            android:label="Add Reminder Activity"
            android:parentActivityName=".ReminderActivity"
            android:theme="@style/Theme.ReminderApp.Toolbar" />

    </application>
</manifest>
